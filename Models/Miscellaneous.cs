using System;
using System.ComponentModel.DataAnnotations;

namespace CoreEMS.Models
{
    public enum Gender {
        MALE,
        FEMALE
    }

    public enum SubjectType {
        OPTIONAL,
        MANDATORY
    }
}